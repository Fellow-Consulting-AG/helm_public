{{- if .Values.service.enabled -}}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "base-api.fullname" . }}-test-connection"
  labels:
    {{- include "base-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "base-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: OnFailure
{{- end }}
